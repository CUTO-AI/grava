/vendor/
/.env
/.phpunit.cache/
/.phpunit.result.cache
/storage/logs/*
!/storage/logs/.gitkeep
/storage/mail/*
!/storage/mail/.gitkeep
/storage/routes/*
!/storage/routes/.gitkeep
# OSM-Grenzen-Zwischendateien der Gebiets-Eroberung (mehrere GB — NIE committen)
/storage/regions/
# Generierte Social-Media-Cards (Cache/Vorschau, jederzeit neu erzeugbar)
/storage/social-cards/
# Generierte OG-Karten (Runtime-Artefakt, jederzeit neu erzeugbar)
/storage/og-images/
.DS_Store
*.log
# Build-Artefakte (z. B. scripts/sync_heatmap_edges.sh export -> build/heatmap_edges.sql)
/build/
# Vom Deploy (CI) geschriebener Versions-Marker für /healthz
/VERSION
# Lokale Sync-/SFTP-Credentials für pull_prod_routes.sh (NIE committen)
/scripts/.env.sync
# Brainstorming-/Visual-Companion-Artefakte
/.superpowers/
# Lokale Ops-/Runbook-Notizen (enthalten Secrets wie INTERNAL_TOKEN — NIE committen)
*.local.md
# Cutover-Material: DB-Dump, alte Prod-.env, APNs-Keys — HOCHSENSIBEL, NIE committen
# (deckt dbs*.sql + .env + *.p8 in dem Ordner ab; migrations/*.sql bleiben getrackt!)
/000_CUTOVER/
# APNs-Signing-Keys gehören generell nie ins Repo
*.p8
